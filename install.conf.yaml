- defaults:
    link:
        create: true
        relink: true

- clean: ['~', '~/.config']

- link:
    # Git
    ~/.gitconfig:
    ~/.config/zsh/: 
      glob: true
      path: zsh/*

- shell:
  - [git submodule update --init --recursive, Installing submodules]